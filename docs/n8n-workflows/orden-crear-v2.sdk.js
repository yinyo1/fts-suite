import { workflow, node, trigger, ifElse } from '@n8n/workflow-sdk';

const webhook = trigger({
  type: 'n8n-nodes-base.webhook', version: 2.1,
  config: { name: 'Webhook', parameters: {
    httpMethod: 'POST', path: 'comercial/orden-crear-v2',
    responseMode: 'responseNode', options: {} } }
});

const setSecreto = node({
  type: 'n8n-nodes-base.set', version: 3.4,
  config: { name: 'Set - secreto', parameters: {
    mode: 'manual', includeOtherFields: true,
    assignments: { assignments: [
      { id: 'a1', name: 'secret', value: '={{ $env.SUITE_JWT_SECRET }}', type: 'string' }
    ] }, options: {} } }
});

const verificar = node({
  type: 'n8n-nodes-base.code', version: 2,
  config: { name: 'Code - Verificar', parameters: { jsCode: "// ── SHA-256 + HMAC-SHA256 + verificacion JWT, JS PURO (sandbox n8n: sin require/crypto) ──\nfunction sha256Bytes(bytes) {\n  const K = [0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,\n    0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,\n    0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,\n    0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,\n    0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,\n    0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,\n    0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,\n    0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2];\n  let H = [0x6a09e667,0xbb67ae85,0x3c6ef372,0xa54ff53a,0x510e527f,0x9b05688c,0x1f83d9ab,0x5be0cd19];\n  const ml = bytes.length * 8;\n  const withPad = bytes.slice();\n  withPad.push(0x80);\n  while (withPad.length % 64 !== 56) withPad.push(0);\n  for (let i = 7; i >= 0; i--) withPad.push((ml / Math.pow(2, i * 8)) & 0xff);\n  const rr = (x, n) => (x >>> n) | (x << (32 - n));\n  for (let i = 0; i < withPad.length; i += 64) {\n    const w = new Array(64);\n    for (let j = 0; j < 16; j++) {\n      w[j] = (withPad[i+j*4] << 24) | (withPad[i+j*4+1] << 16) | (withPad[i+j*4+2] << 8) | withPad[i+j*4+3];\n    }\n    for (let j = 16; j < 64; j++) {\n      const s0 = rr(w[j-15],7) ^ rr(w[j-15],18) ^ (w[j-15] >>> 3);\n      const s1 = rr(w[j-2],17) ^ rr(w[j-2],19) ^ (w[j-2] >>> 10);\n      w[j] = (w[j-16] + s0 + w[j-7] + s1) | 0;\n    }\n    let [a,b,c,d,e,f,g,h] = H;\n    for (let j = 0; j < 64; j++) {\n      const S1 = rr(e,6) ^ rr(e,11) ^ rr(e,25);\n      const ch = (e & f) ^ (~e & g);\n      const t1 = (h + S1 + ch + K[j] + w[j]) | 0;\n      const S0 = rr(a,2) ^ rr(a,13) ^ rr(a,22);\n      const mj = (a & b) ^ (a & c) ^ (b & c);\n      const t2 = (S0 + mj) | 0;\n      h=g; g=f; f=e; e=(d+t1)|0; d=c; c=b; b=a; a=(t1+t2)|0;\n    }\n    H = [(H[0]+a)|0,(H[1]+b)|0,(H[2]+c)|0,(H[3]+d)|0,(H[4]+e)|0,(H[5]+f)|0,(H[6]+g)|0,(H[7]+h)|0];\n  }\n  const out = [];\n  for (const x of H) { out.push((x>>>24)&0xff,(x>>>16)&0xff,(x>>>8)&0xff,x&0xff); }\n  return out;\n}\nfunction strBytes(s) { const o=[]; for (const ch of unescape(encodeURIComponent(s))) o.push(ch.charCodeAt(0)); return o; }\nfunction hmacSha256(keyStr, msgStr) {\n  let key = strBytes(keyStr);\n  if (key.length > 64) key = sha256Bytes(key);\n  while (key.length < 64) key.push(0);\n  const ipad = key.map(b => b ^ 0x36), opad = key.map(b => b ^ 0x5c);\n  return sha256Bytes(opad.concat(sha256Bytes(ipad.concat(strBytes(msgStr)))));\n}\nfunction b64urlFromBytes(bytes) {\n  const A='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_';\n  let out='';\n  for (let i=0;i<bytes.length;i+=3){\n    const b0=bytes[i],b1=bytes[i+1],b2=bytes[i+2];\n    out+=A[b0>>2]; out+=A[((b0&3)<<4)|((b1===undefined?0:b1)>>4)];\n    if(b1!==undefined){ out+=A[((b1&15)<<2)|((b2===undefined?0:b2)>>6)]; if(b2!==undefined) out+=A[b2&63]; }\n  }\n  return out;\n}\nfunction b64urlDecodeToStr(s) {\n  const A='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_';\n  let bits='', out=[];\n  for (const ch of s) { const idx=A.indexOf(ch); if(idx<0) continue; bits+=idx.toString(2).padStart(6,'0'); }\n  for (let i=0;i+8<=bits.length;i+=8) out.push(parseInt(bits.slice(i,i+8),2));\n  return decodeURIComponent(escape(String.fromCharCode.apply(null,out)));\n}\n// Comparacion en tiempo constante\nfunction safeEq(a,b){ if(a.length!==b.length) return false; let d=0; for(let i=0;i<a.length;i++) d|=a.charCodeAt(i)^b.charCodeAt(i); return d===0; }\n\nfunction verifyJWT(token, secret, scopeRequerido) {\n  if (typeof token !== 'string') return { ok:false, error:'TOKEN_AUSENTE' };\n  const p = token.split('.');\n  if (p.length !== 3) return { ok:false, error:'TOKEN_MALFORMADO' };\n  const esperado = b64urlFromBytes(hmacSha256(secret, p[0] + '.' + p[1]));\n  if (!safeEq(esperado, p[2])) return { ok:false, error:'FIRMA_INVALIDA' };\n  let payload;\n  try { payload = JSON.parse(b64urlDecodeToStr(p[1])); } catch(e){ return { ok:false, error:'PAYLOAD_ILEGIBLE' }; }\n  const ahora = Math.floor(Date.now()/1000);\n  if (!payload.exp || payload.exp <= ahora) return { ok:false, error:'TOKEN_EXPIRADO' };\n  // Compatibilidad: token viejo de Finanzas sin 'scopes' => scopes heredados\n  const scopes = Array.isArray(payload.scopes) ? payload.scopes : ['finanzas:read','finanzas:write'];\n  if (scopeRequerido && scopes.indexOf(scopeRequerido) < 0) {\n    return { ok:false, error:'SCOPE_INSUFICIENTE', requerido:scopeRequerido, tiene:scopes };\n  }\n  /* `nombre` y `empleado_id` vienen FIRMADOS dentro del token (auth/suite-login los\n   * mete en el payload). Se devuelven aquí para que ningún endpoint tenga que\n   * volver a leerlos de una tabla —ni, peor, aceptarlos del cuerpo de la petición,\n   * que es como alguien pondría el correo de otro como suyo. */\n  return { ok:true, actor: payload.sub || payload.username || null,\n    nombre: payload.nombre || null,\n    empleado_id: (typeof payload.empleado_id === 'number') ? payload.empleado_id : null,\n    scopes: scopes, exp: payload.exp };\n}\n\n/* ── comercial/orden-crear-v2 · verificar ─────────────────────────────────\n * La cripto de arriba viene RECORTADA de docs/n8n-workflows/fase0/jwt-verify.js\n * (9/9 contra crypto de Node), no transcrita.\n *\n * TODO el cuerpo va en try/catch y devuelve un objeto NUEVO, nunca el item de\n * entrada. El nodo de arriba materializa SUITE_JWT_SECRET, y un nodo que\n * lanza hace que n8n publique su ENTRADA en el payload de error — o sea el\n * secreto en claro, aunque se filtre por nodo al leer la ejecucion\n * (CLAUDE.md §9, ejecucion 90281). Un nodo que no lanza no produce ese\n * payload. Por eso tampoco lleva onError:continueRegularOutput, que seria\n * peor: mandaria el secreto aguas abajo.\n *\n * ── EL SCOPE ──\n * Emitir una orden NO es cotizar. Pide 'comercial:orden' o 'comercial:admin'.\n * El modo de falla es \"nadie puede emitir\" y no \"cualquiera puede\", que es el\n * lado correcto para una escritura real a Odoo.\n */\ntry {\n  const ENTRADA = $input.first().json;\n  const cuerpo  = ENTRADA.body || ENTRADA || {};\n  const secreto = $('Set - secreto').item.json.secret;\n\n  if (!secreto || ('' + secreto).length < 32) {\n    return [{ json: { ok:false, error:'SECRETO_NO_CONFIGURADO',\n      mensaje:'SUITE_JWT_SECRET no esta definida o es muy corta en el entorno de n8n.' } }];\n  }\n\n  const v = verifyJWT(cuerpo.token, '' + secreto);\n  if (!v.ok) {\n    return [{ json: { ok:false, error:v.error,\n      mensaje: v.error === 'TOKEN_EXPIRADO' ? 'La sesion vencio. Vuelve a entrar.'\n             : 'Sesion no valida. Vuelve a entrar.' } }];\n  }\n  const puede = v.scopes.indexOf('comercial:orden') >= 0 || v.scopes.indexOf('comercial:admin') >= 0;\n  if (!puede) {\n    return [{ json: { ok:false, error:'SCOPE_INSUFICIENTE',\n      mensaje:'Tu usuario puede cotizar pero no emitir la orden en Odoo. Pideselo a Esteban.' } }];\n  }\n\n  /* Nada del item de entrada se reenvia: se arma un objeto nuevo con lo que\n   * hace falta rio abajo. Asi el secreto no viaja. */\n  return [{ json: {\n    ok: true,\n    actor: v.actor,\n    actor_nombre: v.nombre || null,\n    es_admin: v.scopes.indexOf('comercial:admin') >= 0,\n    machote_id:   cuerpo.machote_id || null,\n    version_leida: Number(cuerpo.version_leida),\n    lead_id: (cuerpo.lead_id === 0 || cuerpo.lead_id) ? Number(cuerpo.lead_id) : null,\n    /* v2: el DOCUMENTO en orden, y los cinco compromisos. La v1 recibia\n     * `lineas` planas y `a_mano` libre; los dos desaparecieron. */\n    bloques: Array.isArray(cuerpo.bloques) ? cuerpo.bloques : [],\n    compromisos: (cuerpo.compromisos && typeof cuerpo.compromisos === 'object') ? cuerpo.compromisos : {}\n  } }];\n} catch (e) {\n  return [{ json: { ok:false, error:'FALLO_VERIFICACION',\n    mensaje:'No se pudo verificar la sesion.' } }];\n}\n" } }
});

const ifToken = ifElse({
  version: 2.2,
  config: { name: 'IF - Token valido?', parameters: { conditions: {
    options: { caseSensitive: true, leftValue: '', typeValidation: 'loose', version: 2 },
    conditions: [{ id: 'c1', leftValue: '={{ $json.ok }}',
      operator: { type: 'boolean', operation: 'true', singleValue: true } }],
    combinator: 'and' }, options: {} } }
});

const reservar = node({
  type: 'n8n-nodes-base.postgres', version: 2.7,
  config: { name: 'Postgres - Reservar y leer', alwaysOutputData: true,
    credentials: { postgres: { id: '1bfO8GLZoWdMHIf5', name: 'fts-suite-db · comercial_app' } },
    parameters: { resource: 'database', operation: 'executeQuery',
      options: { queryReplacement: '={{ $json.machote_id }}' },
      query: "WITH obj AS (\n  SELECT m.id, m.dueno, m.dueno_nombre, m.folio, m.odoo_so_id, m.odoo_so_name,\n         m.odoo_lead_id, m.archivado_at\n    FROM comercial.machote m\n   WHERE m.id = $1::uuid AND m.deleted_at IS NULL\n),\nult AS (\n  SELECT v.version, v.estado, v.total, v.margen, v.moneda, v.tc,\n         v.documento->>'empresa_id' AS empresa_id,\n         v.documento->>'cliente_id' AS cliente_id,\n         v.documento->>'cliente'    AS cliente_nombre,\n         v.documento->>'nombre'     AS nombre\n    FROM comercial.machote_version v\n   WHERE v.machote_id = (SELECT id FROM obj)\n   ORDER BY v.version DESC\n   LIMIT 1\n),\nres AS (\n  UPDATE comercial.machote m\n     SET odoo_so_claim_at = now()\n    FROM obj o\n   WHERE m.id = o.id\n     AND m.odoo_so_id IS NULL\n     AND (m.odoo_so_claim_at IS NULL\n          OR m.odoo_so_claim_at < now() - interval '2 minutes')\n   RETURNING m.id\n)\nSELECT (SELECT count(*) FROM obj) AS existe,\n       (SELECT count(*) FROM res) AS reservado,\n       o.dueno, o.dueno_nombre, o.folio, o.odoo_so_id, o.odoo_so_name,\n       o.odoo_lead_id, o.archivado_at,\n       u.version, u.estado, u.total, u.margen, u.moneda, u.tc,\n       u.empresa_id, u.cliente_id, u.cliente_nombre, u.nombre\n  FROM (SELECT 1) z\n  LEFT JOIN obj o ON true\n  LEFT JOIN ult u ON true" } }
});

const odooCred = { odooApi: { id: 'Wansi69xesEqEiY1', name: 'Odoo FTS' } };

const getListas = node({
  type: 'n8n-nodes-base.odoo', version: 1,
  config: { name: 'Odoo - getAll listas de precio', credentials: odooCred,
    notes: 'Se traen todas y se elige en Code. Filtrar por moneda aqui obligaria a saber el id de la moneda, y un id de Odoo escrito a mano es el anti-patron de CLAUDE.md 13.',
    parameters: { resource: 'custom', customResource: 'product.pricelist', operation: 'getAll',
      returnAll: true, options: { fieldsList: ['id','name','currency_id','company_id','sequence'] },
      filterRequest: { filter: [{ fieldName: 'active', operator: 'equal', value: '={{ true }}' }] } } }
});

const getImpuestos = node({
  type: 'n8n-nodes-base.odoo', version: 1,
  config: { name: 'Odoo - getAll impuestos', credentials: odooCred,
    notes: 'Las dos empresas de un jalon. El impuesto se elige por NOMBRE y por la empresa que factura, nunca por la moneda: una venta en dolares desde Mexico si lleva IVA.',
    parameters: { resource: 'custom', customResource: 'account.tax', operation: 'getAll',
      returnAll: true, options: { fieldsList: ['id','name','amount','company_id'] },
      filterRequest: { filter: [
        { fieldName: 'type_tax_use', operator: 'equal', value: 'sale' },
        { fieldName: 'active', operator: 'equal', value: '={{ true }}' },
        { fieldName: 'company_id', operator: 'in', value: '={{ [1, 6] }}' } ] } } }
});

const getTerminos = node({
  type: 'n8n-nodes-base.odoo', version: 1,
  config: { name: 'Odoo - getAll terminos de pago', credentials: odooCred,
    notes: 'Los 44 de un jalon, sin filtrar por empresa: el Code descarta los de OTRAS empresas y elige de forma determinista. Filtrar aqui esconderia que el catalogo esta sucio.',
    parameters: { resource: 'custom', customResource: 'account.payment.term', operation: 'getAll',
      returnAll: true, options: { fieldsList: ['id','name','company_id'] },
      filterRequest: { filter: [{ fieldName: 'active', operator: 'equal', value: '={{ true }}' }] } } }
});

const getIncoterms = node({
  type: 'n8n-nodes-base.odoo', version: 1,
  config: { name: 'Odoo - getAll incoterms', credentials: odooCred,
    notes: 'Se guarda el CODIGO (EXW, DDP...) y se resuelve el id aqui: el codigo es estable y universal, el id es de esta base.',
    parameters: { resource: 'custom', customResource: 'account.incoterms', operation: 'getAll',
      returnAll: true, options: { fieldsList: ['id','code','name'] },
      filterRequest: { filter: [{ fieldName: 'active', operator: 'equal', value: '={{ true }}' }] } } }
});

const decidir = node({
  type: 'n8n-nodes-base.code', version: 2,
  config: { name: 'Code - Decidir', parameters: { jsCode: "/* ── comercial/orden-crear-v2 · decidir ───────────────────────────────────\n * Todas las negativas salen por UNA sola puerta, para que todas pasen por el\n * nodo que suelta la reserva. Una negativa que se escape por su cuenta deja\n * el machote reservado dos minutos sin que nadie sepa por que.\n *\n * Lo que se lee del SERVIDOR (no del cuerpo): el dueno, la version vigente,\n * el total congelado de esa version, la moneda, la empresa y el cliente. El\n * cuerpo solo aporta el DOCUMENTO y los COMPROMISOS.\n */\nvar p = $('Code - Verificar').first().json;\nvar r = ($('Postgres - Reservar y leer').first().json) || {};\nvar num = function (x) {\n  if (x === null || x === undefined || x === '') return null;\n  var n = Number(x); return isFinite(n) ? n : null;\n};\nvar reservado = num(r.reservado) > 0;\n\nfunction no(error, mensaje, extra) {\n  var base = { crear: false, ok: false, error: error, mensaje: mensaje,\n               machote_id: p.machote_id, reservado: reservado };\n  return [{ json: Object.assign(base, extra || {}) }];\n}\n\nif (num(r.existe) !== 1) {\n  return no('MACHOTE_NO_ENCONTRADO',\n    'Esa cotizacion no esta en el servidor, o es de alguien mas.');\n}\nif (r.archivado_at) {\n  return no('MACHOTE_ARCHIVADO',\n    'Esta cotizacion esta archivada. Direccion la puede devolver antes de emitir la orden.');\n}\nif (p.actor !== r.dueno && p.es_admin !== true) {\n  return no('NO_ES_TUYO',\n    'Esta cotizacion es de ' + (r.dueno_nombre || r.dueno) + '. Solo su dueno emite la orden.');\n}\nif (num(r.odoo_so_id)) {\n  return [{ json: { crear:false, ok:true, ya_existia:true,\n    machote_id: p.machote_id, reservado: reservado,\n    odoo_so_id: num(r.odoo_so_id), odoo_so_name: r.odoo_so_name,\n    mensaje: 'Esta cotizacion ya tiene su orden en Odoo: ' + r.odoo_so_name + '.' } }];\n}\nif (!reservado) {\n  return no('ORDEN_EN_CURSO',\n    'Alguien esta creando la orden de esta cotizacion en este momento. Espera un minuto y vuelve a mirar.');\n}\n\nvar vServer = num(r.version);\nif (!p.version_leida) {\n  return no('FALTA_VERSION', 'La pantalla no dijo que version esta mirando.');\n}\nif (vServer !== p.version_leida) {\n  var atras = p.version_leida < vServer;\n  return no('CONFLICTO_DE_VERSION',\n    atras\n      ? ('Alguien guardo encima mientras tanto: tu estas viendo la version ' +\n         p.version_leida + ' y el servidor ya va en la ' + vServer +\n         '. Recarga antes de emitir.')\n      : ('La pantalla dice estar en la version ' + p.version_leida +\n         ' y el servidor solo tiene hasta la ' + vServer +\n         '. Recarga: lo que estas viendo no llego a guardarse.'),\n    { version_leida: p.version_leida, version_servidor: vServer });\n}\n\nvar clienteId = num(r.cliente_id);\nif (!clienteId) {\n  return no('SIN_CLIENTE',\n    'Esta cotizacion no tiene cliente de Odoo. Eligelo de la lista de clientes (no basta con teclear el nombre) y vuelve a guardar.');\n}\nvar empresaId = num(r.empresa_id);\nif (!empresaId) {\n  return no('SIN_EMPRESA', 'Esta cotizacion no dice con que empresa se factura.');\n}\nvar moneda = String(r.moneda || '');\nif (moneda !== 'MXN' && moneda !== 'USD') {\n  return no('MONEDA_INVALIDA',\n    'La moneda del machote (' + (moneda || 'vacia') + ') no es ni MXN ni USD.');\n}\n\n/* ══ LOS CINCO COMPROMISOS ═══════════════════════════════════════════════\n * Esta es la razon de ser de la v2. Medido sobre las 176 ordenes confirmadas\n * de 2025-2026: 49 sin terminos de pago, CERO con incoterm, CERO con fecha\n * comprometida, UNA con vigencia escrita. Y la mas grande sin terminos de\n * pago es SO11771, de 45,854,038.88 MXN.\n *\n * El compromiso si existe: vive dentro del texto de las notas. Ahi nadie lo\n * puede consultar, ni sumar, ni vigilar. Aqui deja de ser opcional.\n *\n * Se validan EN EL SERVIDOR y no solo en la pantalla, porque una validacion\n * que solo vive en el navegador se salta con una peticion a mano — y este\n * endpoint escribe a produccion.\n */\nvar c = p.compromisos || {};\nvar faltan = [];\nvar pagoDias = num(c.pago_dias);\nif (pagoDias === null) faltan.push('terminos de pago');\nif (!c.incoterm_code) faltan.push('terminos comerciales (incoterm)');\nif (!c.entrega_texto || !String(c.entrega_texto).trim()) faltan.push('tiempo de entrega');\nvar vigDias = num(c.vigencia_dias);\nif (!vigDias) faltan.push('vigencia de la cotizacion');\nif (faltan.length) {\n  return no('FALTAN_COMPROMISOS',\n    'No se puede emitir sin los cinco compromisos comerciales. Faltan: ' +\n    faltan.join(', ') + '.', { faltan: faltan });\n}\nif (pagoDias < 0 || pagoDias > 730) {\n  return no('PAGO_DIAS_INVALIDO', 'El plazo de credito (' + pagoDias + ' dias) no es razonable.');\n}\nif (vigDias < 1 || vigDias > 365) {\n  return no('VIGENCIA_INVALIDA', 'La vigencia (' + vigDias + ' dias) no es razonable.');\n}\nif (!/^[A-Z][A-Z][A-Z]$/.test(String(c.incoterm_code))) {\n  return no('INCOTERM_INVALIDO',\n    'El incoterm \"' + c.incoterm_code + '\" no es un codigo de tres letras mayusculas.');\n}\n/* La MONEDA del cuerpo no manda: manda la del machote, que es la que se leyo\n * del servidor. Si la pantalla dice otra cosa, la pantalla esta mirando algo\n * viejo y hay que pararlo — no corregirlo en silencio. */\nif (c.moneda && String(c.moneda) !== moneda) {\n  return no('MONEDA_DISCREPA',\n    'La pantalla dice ' + c.moneda + ' y el machote guardado dice ' + moneda +\n    '. Recarga antes de emitir.');\n}\n/* Los hitos son opcionales; si los hay tienen que sumar 100. Un reparto que\n * suma 90 es un error que el cliente descubre cobrando, no nosotros aqui. */\nvar hitos = Array.isArray(c.pago_hitos) ? c.pago_hitos : [];\nif (hitos.length) {\n  var sumaH = hitos.reduce(function (a, h) { return a + (num(h.porcentaje) || 0); }, 0);\n  if (Math.abs(sumaH - 100) > 0.001) {\n    return no('HITOS_NO_SUMAN',\n      'Los hitos de pago suman ' + (Math.round(sumaH * 100) / 100) + '% y tienen que sumar 100%.',\n      { suma_hitos: sumaH });\n  }\n}\n\n/* ══ EL DOCUMENTO ════════════════════════════════════════════════════════\n * Tres tipos de bloque, y el ORDEN del arreglo es el orden de la orden.\n *   line_section → encabezado: sin producto, sin cantidad, sin precio\n *   line_note    → la nota: donde vive el contrato\n *   null         → la linea con precio, que SI lleva producto\n */\nvar bloques = Array.isArray(p.bloques) ? p.bloques : [];\nif (!bloques.length) {\n  return no('SIN_DOCUMENTO', 'La orden vendria sin un solo renglon.');\n}\nvar limpios = [];\nvar conPrecio = 0;\nfor (var i = 0; i < bloques.length; i++) {\n  var b = bloques[i] || {};\n  var nombre = String(b.nombre || '').trim();\n  if (!nombre) return no('BLOQUE_SIN_TEXTO', 'El renglon ' + (i + 1) + ' no tiene texto.');\n  var dt = b.display_type || null;\n  if (dt === 'line_section' || dt === 'line_note') {\n    limpios.push({ display_type: dt, nombre: nombre.slice(0, 4000) });\n    continue;\n  }\n  if (dt !== null && dt !== undefined) {\n    return no('DISPLAY_TYPE_DESCONOCIDO',\n      'El renglon ' + (i + 1) + ' dice ser de tipo \"' + dt + '\", que no existe.');\n  }\n  var cant = num(b.cantidad), pu = num(b.precio);\n  if (cant === null || pu === null) {\n    return no('LINEA_SIN_NUMEROS', 'El renglon ' + (i + 1) + ' no trae cantidad o precio.');\n  }\n  conPrecio++;\n  limpios.push({ display_type: null, nombre: nombre.slice(0, 4000), cantidad: cant, precio: pu });\n}\nif (!conPrecio) {\n  return no('SIN_LINEAS_CON_PRECIO',\n    'El documento no tiene ninguna linea con precio. Una orden sin importe no es una orden.');\n}\n\nvar suma = limpios.reduce(function (a, l) {\n  return l.display_type === null ? a + (l.cantidad * l.precio) : a;\n}, 0);\nvar total = num(r.total);\nif (total === null) {\n  return no('MACHOTE_SIN_TOTAL', 'La version guardada no trae total. Vuelve a guardar la cotizacion.');\n}\n\n/* ── EL CUADRE, Y POR QUE CAMBIO RESPECTO A LA v1 ──────────────────────────\n * La v1 exigia que las lineas sumaran EXACTAMENTE el total del machote. En la\n * v2 el documento se edita libremente antes de emitir —es el punto del\n * cambio— asi que esa igualdad ya no se puede exigir.\n *\n * Pero las dos direcciones NO son iguales, y por eso no se tratan igual:\n *   · Por ARRIBA se cobra mas de lo cotizado. Mejora el margen, no rompe\n *     ninguna aprobacion: se deja pasar y se avisa.\n *   · Por ABAJO se cobra menos, y eso cambia el margen sobre el que la\n *     compuerta ya dio el visto bueno. Dejarlo pasar seria una forma de\n *     esquivar la aprobacion sin tocar el machote. Se bloquea.\n * El camino correcto para bajar el precio es bajarlo en la cotizacion y\n * volver a pasar por la compuerta, no editarlo en la ultima pantalla.\n */\nif (suma <= 0) {\n  return no('TOTAL_EN_CERO', 'El documento suma ' + suma.toFixed(2) + '. No se emite una orden en cero.');\n}\nif (suma < total - 0.005) {\n  return no('PRECIO_A_LA_BAJA',\n    'El documento suma ' + suma.toFixed(2) + ' y la cotizacion aprobada dice ' + total.toFixed(2) +\n    '. Cobrar menos de lo cotizado cambia el margen que ya se aprobo: bajalo en la cotizacion ' +\n    'y vuelve a pasar por la compuerta, no aqui.',\n    { suma_lineas: suma, total_machote: total });\n}\n\nreturn [{ json: {\n  crear: true, ok: true,\n  machote_id: p.machote_id, reservado: reservado,\n  actor: p.actor, actor_nombre: p.actor_nombre,\n  folio: num(r.folio), nombre: r.nombre || null,\n  version: vServer, empresa_id: empresaId, cliente_id: clienteId,\n  cliente_nombre: r.cliente_nombre || null,\n  moneda: moneda, tc: num(r.tc), margen: num(r.margen), total: total,\n  lead_id: p.lead_id || null,\n  bloques: limpios, suma_documento: suma,\n  compromisos: {\n    pago_dias: pagoDias,\n    pago_termino_texto: c.pago_termino_texto || null,\n    pago_hitos: hitos,\n    incoterm_code: String(c.incoterm_code),\n    entrega_texto: String(c.entrega_texto).trim(),\n    entrega_fecha: c.entrega_fecha || null,\n    vigencia_dias: vigDias,\n    vigencia_hasta: c.vigencia_hasta || null\n  }\n} }];\n" } }
});

const ifCrear = ifElse({
  version: 2.2,
  config: { name: 'IF - Crear?', parameters: { conditions: {
    options: { caseSensitive: true, leftValue: '', typeValidation: 'loose', version: 2 },
    conditions: [{ id: 'c1', leftValue: '={{ $json.crear }}',
      operator: { type: 'boolean', operation: 'true', singleValue: true } }],
    combinator: 'and' }, options: {} } }
});

const productos = node({
  type: 'n8n-nodes-base.code', version: 2,
  config: { name: 'Code - Productos a crear', parameters: { jsCode: "/* ── comercial/orden-crear-v2 · un producto por linea con precio ──────────\n * Emite UN ITEM por linea con precio, para que el nodo Odoo de abajo corra\n * una vez por cada uno. Los bloques de seccion y de nota NO pasan por aqui:\n * no llevan producto, y por eso mismo no chocan con la restriccion que\n * impide confirmar una orden cuyas lineas con precio no lo traen.\n *\n * ── EL RIESGO, DICHO EN VOZ ALTA ──────────────────────────────────────────\n * Crear un producto por cotizacion infla el catalogo. Ya esta pasando: hay\n * 14,868 productos, 2,870 creados en 2026 y 2,334 de esos (81%) nunca han\n * estado en una linea de venta. Esto NO lo arregla — reproduce lo que el\n * equipo hace a mano, que es lo que se pidio, porque es la forma que confirma.\n *\n * Lo unico que se agrega es TRAZA: `default_code` lleva el folio de la\n * cotizacion que lo creo, para que despues se pueda contestar \"de donde\n * salio este producto\" y limpiar por lote. Hoy esa pregunta no tiene\n * respuesta, y por eso nadie ha podido limpiar nada.\n */\nvar d = $('Code - Decidir').first().json;\nvar folio = d.folio ? ('COT-' + d.folio) : 'COT-SIN-FOLIO';\n\nvar out = [];\nd.bloques.forEach(function (b, i) {\n  if (b.display_type !== null) return;\n  out.push({ json: {\n    pos: i,\n    /* El NOMBRE del producto es el texto del alcance. Asi lo hacen hoy a\n     * mano, y es lo que el cliente lee en la orden. Odoo corta el name de\n     * product a 4000; el limite real util es el del PDF. */\n    nombre: b.nombre,\n    precio: b.precio,\n    cantidad: b.cantidad,\n    codigo: folio + '-' + (out.length + 1)\n  } });\n});\nreturn out;\n" } }
});

const crearProducto = node({
  type: 'n8n-nodes-base.odoo', version: 1,
  config: { name: 'Odoo - CREATE producto', credentials: odooCred,
    onError: 'continueRegularOutput',
    notes: 'Corre UNA VEZ POR LINEA con precio. onError:continueRegularOutput es seguro aqui: su entrada es un objeto nuevo de Code - Productos a crear, no el item que trae el secreto. Sin esto, un rechazo de Odoo devolveria un 500 sin nuestro JSON y la reserva se quedaria puesta.',
    parameters: { resource: 'custom', customResource: 'product.product',
      fieldsToCreateOrUpdate: { fields: [
        { fieldName: 'name', fieldValue: '={{ $json.nombre }}' },
        { fieldName: 'type', fieldValue: 'service' },
        { fieldName: 'sale_ok', fieldValue: '={{ true }}' },
        { fieldName: 'purchase_ok', fieldValue: '={{ false }}' },
        { fieldName: 'list_price', fieldValue: '={{ $json.precio }}' },
        { fieldName: 'default_code', fieldValue: '={{ $json.codigo }}' } ] } } }
});

const armar = node({
  type: 'n8n-nodes-base.code', version: 2,
  config: { name: 'Code - Armar orden', parameters: { jsCode: "/* ── comercial/orden-crear-v2 · armar la orden ────────────────────────────\n * Cuatro reglas separadas, y no se mezclan:\n *\n *   1. La MONEDA la manda el machote. La lista de precios se elige POR ESA\n *      MONEDA, nunca se deja que Odoo escoja (medido: SO11886 nacio en\n *      dolares siendo de company 1, sin un solo error).\n *   2. El IMPUESTO lo decide la EMPRESA que factura, no la moneda. Una venta\n *      en dolares desde Mexico SI lleva IVA 16% (SO11699).\n *   3. El TERMINO DE PAGO se resuelve por DIAS contra el catalogo real de\n *      Odoo, prefiriendo el termino de la empresa que factura.\n *   4. El INCOTERM se resuelve por CODIGO, que es estable y universal.\n */\nvar d = $('Code - Decidir').first().json;\nvar pedidos = $('Code - Productos a crear').all().map(function (i) { return i.json; });\nvar creados = $input.all().map(function (i) { return i.json; });\n\nvar listas    = $('Odoo - getAll listas de precio').all().map(function (i) { return i.json; });\nvar impuestos = $('Odoo - getAll impuestos').all().map(function (i) { return i.json; });\nvar terminos  = $('Odoo - getAll terminos de pago').all().map(function (i) { return i.json; });\nvar incoterms = $('Odoo - getAll incoterms').all().map(function (i) { return i.json; });\n\nvar IVA_POR_EMPRESA = { 1: 'IVA(16%) VENTAS', 6: null };\n\nvar m2oId  = function (v) { return Array.isArray(v) ? Number(v[0]) : (v === false || v === null || v === undefined ? null : Number(v)); };\nvar m2oNom = function (v) { return Array.isArray(v) ? String(v[1] || '') : ''; };\nfunction mal(error, mensaje, extra) {\n  return [{ json: Object.assign({ armo: false, error: error, mensaje: mensaje }, extra || {}) }];\n}\n\n/* ── LOS PRODUCTOS ──\n * El `create` de Odoo devuelve el id, pero un id devuelto NO prueba que los\n * campos se guardaron (CLAUDE.md §9). Aqui solo se comprueba lo minimo que\n * hace falta para seguir: que hay tantos ids como lineas con precio. Si falta\n * uno, se para — emitir la orden con una linea sin producto la dejaria sin\n * poder confirmarse, que es exactamente el problema que esta v2 viene a\n * resolver. */\nvar ids = creados.map(function (x) { return Number(x && x.id); }).filter(function (n) { return !!n; });\nif (ids.length !== pedidos.length) {\n  return mal('PRODUCTOS_INCOMPLETOS',\n    'Odoo creo ' + ids.length + ' productos de los ' + pedidos.length + ' que la orden necesita. ' +\n    'No se emite una orden con lineas sin producto: no se podria confirmar.',\n    { productos_pedidos: pedidos.length, productos_creados: ids.length });\n}\n\n/* ── LA LISTA DE PRECIOS ── */\nvar candidatas = listas.filter(function (pl) {\n  var c = m2oId(pl.company_id);\n  return m2oNom(pl.currency_id) === d.moneda && (c === null || c === d.empresa_id);\n});\ncandidatas.sort(function (a, b) {\n  var ca = m2oId(a.company_id) === d.empresa_id ? 0 : 1;\n  var cb = m2oId(b.company_id) === d.empresa_id ? 0 : 1;\n  if (ca !== cb) return ca - cb;\n  var sa = Number(a.sequence || 0), sb = Number(b.sequence || 0);\n  if (sa !== sb) return sa - sb;\n  return Number(a.id) - Number(b.id);\n});\nvar lista = candidatas[0] || null;\nif (!lista) {\n  return mal('SIN_LISTA_DE_PRECIOS',\n    'No hay lista de precios en ' + d.moneda + ' para esa empresa. ' +\n    'Antes de inventar una, se prefiere no emitir la orden.');\n}\n\n/* ── EL IMPUESTO ── */\nvar nombreIva = IVA_POR_EMPRESA[String(d.empresa_id)];\nvar taxIds = [];\nif (nombreIva) {\n  var enc = impuestos.filter(function (t) {\n    return String(t.name || '').trim() === nombreIva && m2oId(t.company_id) === d.empresa_id;\n  })[0];\n  if (!enc) {\n    return mal('SIN_IMPUESTO',\n      'No se encontro el impuesto \"' + nombreIva + '\" de esa empresa en Odoo. ' +\n      'Emitir sin IVA seria peor que no emitir.');\n  }\n  taxIds = [Number(enc.id)];\n}\n\n/* ── EL TERMINO DE PAGO ───────────────────────────────────────────────────\n * El catalogo de Odoo tiene 44 terminos y esta sucio: \"30 days\" dos veces con\n * ids distintos (31 y 32), un termino llamado \".\", y terminos de empresas que\n * no son ninguna de las dos nuestras. La limpieza es de Esteban y de Gerardo\n * y va aparte; aqui solo se navega lo que hay, de forma DETERMINISTA:\n *\n *   · Se leen los dias del PRINCIPIO del nombre, y solo si van seguidos de\n *     una palabra de dias. Asi \"30% Now, Balance 60 Days\" NO cuenta como 30:\n *     despues del 30 viene un \"%\", no \"days\".\n *   · Se descartan los terminos de OTRAS empresas (los de YIN, los del\n *     contribuyente persona fisica). Un termino de otra empresa en nuestra\n *     orden es un error contable silencioso.\n *   · Gana el de la empresa que factura; si no hay, el que no tiene empresa;\n *     y entre iguales, el id mas chico — para que dos corridas del mismo\n *     machote elijan siempre el mismo y no se vuelva una loteria.\n */\nfunction diasDelNombre(n) {\n  var m = /^\\s*([0-9]+)\\s*(d|day|days|dia|dias|día|días)\\b/i.exec(String(n || ''));\n  return m ? Number(m[1]) : null;\n}\nfunction esInmediato(n) { return /immediate|inmediate|inmediatly|contado/i.test(String(n || '')); }\n\nvar tCand = terminos.filter(function (t) {\n  var c = m2oId(t.company_id);\n  if (c !== null && c !== d.empresa_id) return false;      // de otra empresa: fuera\n  return d.compromisos.pago_dias === 0\n    ? esInmediato(t.name)\n    : diasDelNombre(t.name) === d.compromisos.pago_dias;\n});\ntCand.sort(function (a, b) {\n  var ca = m2oId(a.company_id) === d.empresa_id ? 0 : 1;\n  var cb = m2oId(b.company_id) === d.empresa_id ? 0 : 1;\n  if (ca !== cb) return ca - cb;\n  return Number(a.id) - Number(b.id);\n});\nvar termino = tCand[0] || null;\nif (!termino) {\n  /* Se para en vez de emitir sin terminos. Es la direccion correcta del\n   * fallo: no emitir es molesto, emitir sin terminos es justo lo que se midio\n   * en 49 de 176 ordenes y lo que esta v2 viene a impedir. */\n  var hay = terminos.filter(function (t) {\n    var c = m2oId(t.company_id); return c === null || c === d.empresa_id;\n  }).map(function (t) { return t.name; }).slice(0, 25);\n  return mal('SIN_TERMINO_DE_PAGO',\n    'No hay en Odoo un termino de pago de ' + d.compromisos.pago_dias + ' dias para esa empresa. ' +\n    'Emitir sin terminos seria peor que no emitir.',\n    { plazo_pedido: d.compromisos.pago_dias, terminos_disponibles: hay });\n}\n\n/* ── EL INCOTERM ── */\nvar inc = incoterms.filter(function (x) {\n  return String(x.code || '').toUpperCase() === d.compromisos.incoterm_code;\n})[0];\nif (!inc) {\n  return mal('SIN_INCOTERM',\n    'El incoterm ' + d.compromisos.incoterm_code + ' no esta en el catalogo de Odoo.',\n    { incotermos_en_odoo: incoterms.map(function (x) { return x.code; }) });\n}\n\n/* ── LAS LINEAS ───────────────────────────────────────────────────────────\n * La forma real de FTS: encabezado de seccion sin producto ni precio, linea\n * con precio Y con producto, y las notas. `sequence` va explicito porque el\n * ORDEN es el documento: sin el, Odoo reordena y las notas dejan de estar\n * debajo de su tramo. */\nvar k = 0, seq = 10;\nvar order_line = d.bloques.map(function (b) {\n  seq += 10;\n  if (b.display_type === 'line_section' || b.display_type === 'line_note') {\n    return [0, 0, { display_type: b.display_type, name: b.nombre, sequence: seq }];\n  }\n  var prod = Number(creados[k] && creados[k].id); k++;\n  return [0, 0, { product_id: prod, name: b.nombre,\n                  product_uom_qty: b.cantidad, price_unit: b.precio,\n                  tax_ids: [[6, 0, taxIds]], sequence: seq }];\n});\n\n/* ── LA NOTA AL PIE ───────────────────────────────────────────────────────\n * Los compromisos tambien van en `note`, ademas de en sus campos. No es\n * duplicar por gusto: los campos son para poder SUMAR (cuanto hay vendido a\n * 120 dias, que cotizacion vencio) y la nota es para que el cliente los LEA\n * en el PDF. Los hitos, ademas, no caben en `payment_term_id` de Odoo, asi\n * que la nota es su unico camino al cliente. */\nvar esc = function (s) {\n  return String(s === null || s === undefined ? '' : s)\n    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');\n};\nvar cp = d.compromisos;\nvar t = [];\nt.push('<p><b>Terminos de pago:</b> ' + esc(cp.pago_termino_texto || termino.name) + '</p>');\nif (cp.pago_hitos.length) {\n  t.push('<p><b>Forma de pago:</b><br>' + cp.pago_hitos.map(function (h) {\n    return esc((Number(h.porcentaje) || 0) + '% ' + (h.concepto || '') +\n               (h.cuando ? ' (' + h.cuando + ')' : ''));\n  }).join('<br>') + '</p>');\n}\nt.push('<p><b>Terminos comerciales:</b> ' + esc(cp.incoterm_code + ' - ' + (inc.name || '')) + '</p>');\nt.push('<p><b>Tiempo de entrega:</b> ' + esc(cp.entrega_texto) +\n       (cp.entrega_fecha ? esc(' (comprometida el ' + cp.entrega_fecha + ')') : '') + '</p>');\n\nvar venceEl = String(cp.vigencia_hasta || '').trim();\nif (!/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/.test(venceEl)) {\n  var f = new Date(); f.setDate(f.getDate() + cp.vigencia_dias);\n  venceEl = f.getFullYear() + '-' + ('0' + (f.getMonth() + 1)).slice(-2) + '-' + ('0' + f.getDate()).slice(-2);\n}\nt.push('<p><b>Vigencia de esta cotizacion:</b> ' + esc(venceEl) + '</p>');\n\n/* `commitment_date` es DATETIME en Odoo, no date. Se manda a mediodia para\n * que ninguna conversion de zona la corra al dia anterior. */\nvar entregaDt = /^[0-9]{4}-[0-9]{2}-[0-9]{2}$/.test(String(cp.entrega_fecha || ''))\n  ? (cp.entrega_fecha + ' 12:00:00') : false;\n\nreturn [{ json: {\n  armo: true,\n  machote_id: d.machote_id,\n  partner_id: d.cliente_id,\n  company_id: d.empresa_id,\n  pricelist_id: Number(lista.id),\n  pricelist_nombre: lista.name,\n  payment_term_id: Number(termino.id),\n  payment_term_nombre: termino.name,\n  incoterm: Number(inc.id),\n  incoterm_code: cp.incoterm_code,\n  commitment_date: entregaDt,\n  opportunity_id: d.lead_id ? Number(d.lead_id) : false,\n  validity_date: venceEl,\n  note: t.join(''),\n  order_line: order_line,\n  tax_ids_usados: taxIds,\n  productos_creados: ids,\n  moneda_esperada: d.moneda,\n  total_esperado: d.suma_documento,\n  total_machote: d.total\n} }];\n" } }
});

const ifArmo = ifElse({
  version: 2.2,
  config: { name: 'IF - Se armo?', parameters: { conditions: {
    options: { caseSensitive: true, leftValue: '', typeValidation: 'loose', version: 2 },
    conditions: [{ id: 'c1', leftValue: '={{ $json.armo }}',
      operator: { type: 'boolean', operation: 'true', singleValue: true } }],
    combinator: 'and' }, options: {} } }
});

const crearOrden = node({
  type: 'n8n-nodes-base.odoo', version: 1,
  config: { name: 'Odoo - CREATE orden', credentials: odooCred,
    onError: 'continueRegularOutput',
    parameters: { resource: 'custom', customResource: 'sale.order',
      fieldsToCreateOrUpdate: { fields: [
        { fieldName: 'partner_id', fieldValue: '={{ $json.partner_id }}' },
        { fieldName: 'company_id', fieldValue: '={{ $json.company_id }}' },
        { fieldName: 'pricelist_id', fieldValue: '={{ $json.pricelist_id }}' },
        { fieldName: 'payment_term_id', fieldValue: '={{ $json.payment_term_id }}' },
        { fieldName: 'incoterm', fieldValue: '={{ $json.incoterm }}' },
        { fieldName: 'commitment_date', fieldValue: '={{ $json.commitment_date }}' },
        { fieldName: 'opportunity_id', fieldValue: '={{ $json.opportunity_id }}' },
        { fieldName: 'validity_date', fieldValue: '={{ $json.validity_date }}' },
        { fieldName: 'note', fieldValue: '={{ $json.note }}' },
        { fieldName: 'order_line', fieldValue: '={{ $json.order_line }}' } ] } } }
});

const queDevolvio = node({
  type: 'n8n-nodes-base.code', version: 2,
  config: { name: 'Code - Que devolvio Odoo', parameters: { jsCode: "/* El id se normaliza a numero: un many2one que viaja como texto se anula en\n * silencio (CLAUDE.md 20 #6). Y si no hay id, Odoo rechazo: se dice con el\n * texto de Odoo tal cual, que es lo unico que ayuda a entender por que. */\nvar r = $input.first().json || {};\nvar id = Number(r.id);\nif (id) return [{ json: { creo: true, so_id: id } }];\n\nvar detalle = r.error || r.message || (r.json && r.json.error) || '';\nif (detalle && typeof detalle === 'object') detalle = JSON.stringify(detalle);\nreturn [{ json: { creo: false, ok: false, error: 'ODOO_RECHAZO',\n  mensaje: 'Odoo no acepto la orden. ' + String(detalle || 'No dijo por que.').slice(0, 400) } }];\n" } }
});

const ifSeCreo = ifElse({
  version: 2.2,
  config: { name: 'IF - Se creo?', parameters: { conditions: {
    options: { caseSensitive: true, leftValue: '', typeValidation: 'loose', version: 2 },
    conditions: [{ id: 'c1', leftValue: '={{ $json.creo }}',
      operator: { type: 'boolean', operation: 'true', singleValue: true } }],
    combinator: 'and' }, options: {} } }
});

const releer = node({
  type: 'n8n-nodes-base.odoo', version: 1,
  config: { name: 'Odoo - Releer la orden', credentials: odooCred,
    notes: 'El id que devuelve el create no prueba que los campos se guardaron: Odoo descarta en silencio lo que no puede escribir (CLAUDE.md 9). Los cinco compromisos se leen de vuelta, uno por uno.',
    parameters: { resource: 'custom', customResource: 'sale.order', operation: 'getAll',
      returnAll: true, options: { fieldsList: ['id','name','state','company_id','currency_id',
        'pricelist_id','amount_untaxed','amount_tax','amount_total','order_line',
        'payment_term_id','incoterm','commitment_date','validity_date','opportunity_id','note'] },
      filterRequest: { filter: [{ fieldName: 'id', operator: 'equal', value: '={{ $json.so_id }}' }] } } }
});

const guardar = node({
  type: 'n8n-nodes-base.postgres', version: 2.7,
  config: { name: 'Postgres - Guardar la orden',
    credentials: { postgres: { id: '1bfO8GLZoWdMHIf5', name: 'fts-suite-db · comercial_app' } },
    notes: 'El AND odoo_so_id IS NULL es la segunda mitad de la reserva: si otro gano la carrera, esta escritura no pisa.',
    parameters: { resource: 'database', operation: 'executeQuery',
      options: { queryReplacement: "={{ $('Code - Decidir').first().json.machote_id }},{{ $json.id }},{{ $json.name }},{{ $('Code - Decidir').first().json.actor }},{{ $('Code - Decidir').first().json.lead_id || 0 }},{{ $('Code - Decidir').first().json.cliente_id || 0 }}" },
      query: "UPDATE comercial.machote\n   SET odoo_so_id       = $2::int,\n       odoo_so_name     = $3,\n       orden_creada_at  = now(),\n       orden_creada_por = $4,\n       odoo_so_claim_at = NULL,\n       odoo_lead_id     = COALESCE(NULLIF($5::int, 0), odoo_lead_id),\n       odoo_partner_id  = COALESCE(NULLIF($6::int, 0), odoo_partner_id),\n       updated_by       = $4\n WHERE id = $1::uuid\n   AND deleted_at IS NULL\n   AND odoo_so_id IS NULL\nRETURNING id, odoo_so_id, odoo_so_name, odoo_lead_id, odoo_partner_id,\n          orden_creada_at, orden_creada_por" } }
});

const resultado = node({
  type: 'n8n-nodes-base.code', version: 2,
  config: { name: 'Code - Resultado', parameters: { jsCode: "/* El resultado se pinta del READ-BACK de Odoo y del RETURNING de la base, no\n * de que los nodos de arriba dijeran que si. */\nvar d = $('Code - Decidir').first().json;\nvar a = $('Code - Armar orden').first().json;\nvar o = ($('Odoo - Releer la orden').first().json) || {};\nvar g = $input.first().json || {};\n\nvar num = function (x) { var n = Number(x); return isFinite(n) ? n : null; };\nvar m2oNom = function (v) { return Array.isArray(v) ? String(v[1] || '') : null; };\nvar m2oId  = function (v) { return Array.isArray(v) ? Number(v[0]) : null; };\n\nvar subtotal = num(o.amount_untaxed);\nvar esperado = num(d.suma_documento);\nvar cuadra = (subtotal !== null && esperado !== null && Math.abs(subtotal - esperado) < 0.005);\nvar moneda = m2oNom(o.currency_id);\nvar monedaOk = (moneda === d.moneda);\nvar guardado = !!(g && g.odoo_so_id);\n\n/* ── LO QUE SE RELEE, Y POR QUE ──────────────────────────────────────────\n * No basta con que la orden exista: los CINCO COMPROMISOS tienen que haber\n * quedado escritos. Odoo acepta un create y descarta en silencio los campos\n * que no puede escribir (CLAUDE.md §9), asi que cada uno se comprueba contra\n * lo que Odoo devolvio, no contra lo que se le mando. Si alguno se cayo, la\n * orden existe pero vuelve a ser una de las 176 sin compromisos — y eso hay\n * que decirlo, no esconderlo. */\nvar avisos = [];\nif (!cuadra) {\n  avisos.push('El subtotal de la orden (' + subtotal + ') no cuadra con el documento (' + esperado + ').');\n}\nif (!monedaOk) {\n  avisos.push('La orden nacio en ' + moneda + ' y el machote dice ' + d.moneda + '.');\n}\nif (!guardado) {\n  avisos.push('La orden existe en Odoo pero la cotizacion no quedo ligada a ella en la base.');\n}\nif (m2oId(o.payment_term_id) !== a.payment_term_id) {\n  avisos.push('Los terminos de pago no quedaron escritos en la orden.');\n}\nif (m2oId(o.incoterm) !== a.incoterm) {\n  avisos.push('El incoterm no quedo escrito en la orden.');\n}\nif (a.commitment_date && !o.commitment_date) {\n  avisos.push('La fecha comprometida de entrega no quedo escrita en la orden.');\n}\nif (!o.validity_date) {\n  avisos.push('La vigencia no quedo escrita en la orden.');\n}\n\n/* Cuantos renglones de cada tipo quedaron. Es la prueba de que la FORMA\n * llego: secciones, lineas con precio y notas. */\nvar forma = { secciones: 0, con_precio: 0, notas: 0 };\nd.bloques.forEach(function (b) {\n  if (b.display_type === 'line_section') forma.secciones++;\n  else if (b.display_type === 'line_note') forma.notas++;\n  else forma.con_precio++;\n});\n\nvar dif = (esperado !== null && num(d.total) !== null) ? (esperado - num(d.total)) : null;\nif (dif !== null && dif > 0.005) {\n  avisos.push('El documento cobra ' + dif.toFixed(2) + ' mas que la cotizacion aprobada. ' +\n              'No esta mal, pero conviene que alguien lo sepa.');\n}\n\nreturn [{ json: {\n  ok: true,\n  orden_creada: true,\n  odoo_so_id: num(o.id),\n  odoo_so_name: o.name || null,\n  estado: o.state || null,\n  moneda: moneda,\n  lista_precios: m2oNom(o.pricelist_id),\n  empresa: m2oNom(o.company_id),\n  subtotal: subtotal,\n  impuesto: num(o.amount_tax),\n  total: num(o.amount_total),\n  vence_el: o.validity_date || null,\n  lineas: Array.isArray(o.order_line) ? o.order_line.length : null,\n  forma: forma,\n  compromisos_en_odoo: {\n    terminos_pago: m2oNom(o.payment_term_id),\n    incoterm: m2oNom(o.incoterm),\n    entrega_comprometida: o.commitment_date || null,\n    vigencia: o.validity_date || null,\n    moneda: moneda\n  },\n  productos_creados: a.productos_creados || [],\n  total_machote: num(d.total),\n  total_documento: esperado,\n  cuadra: cuadra,\n  moneda_correcta: monedaOk,\n  ligada_en_la_base: guardado,\n  odoo_lead_id: (g && g.odoo_lead_id) || null,\n  avisos: avisos,\n  mensaje: avisos.length\n    ? ('La orden ' + (o.name || '') + ' se creo, pero hay que mirarla: ' + avisos.join(' '))\n    : ('Orden ' + (o.name || '') + ' creada en Odoo, en borrador, con ' +\n       forma.secciones + ' secciones, ' + forma.con_precio + ' lineas con precio y ' +\n       forma.notas + ' notas.')\n} }];\n" } }
});

const soltar = node({
  type: 'n8n-nodes-base.postgres', version: 2.7,
  config: { name: 'Postgres - Soltar reserva',
    credentials: { postgres: { id: '1bfO8GLZoWdMHIf5', name: 'fts-suite-db · comercial_app' } },
    notes: 'Todas las negativas pasan por aqui. Si no llegamos a reservar, el $2 falso lo vuelve un no-op: soltar una reserva ajena seria peor que no soltar la propia.',
    parameters: { resource: 'database', operation: 'executeQuery',
      options: { queryReplacement: "={{ $('Code - Decidir').first().json.machote_id }},{{ $('Code - Decidir').first().json.reservado === true }}" },
      query: "UPDATE comercial.machote\n   SET odoo_so_claim_at = NULL\n WHERE id = $1::uuid\n   AND deleted_at IS NULL\n   AND odoo_so_id IS NULL\n   AND $2::boolean\nRETURNING id" } }
});

const noSeCreo = node({
  type: 'n8n-nodes-base.code', version: 2,
  config: { name: 'Code - No se creo', parameters: { jsCode: "/* Tres caminos llegan aqui — la decision dijo que no, el armado no encontro\n * lista, impuesto, termino o incoterm, u Odoo rechazo — y cada uno tiene su\n * motivo en su propio nodo. Se busca del mas tardio al mas temprano, y los\n * `try` son porque un nodo que no corrio LANZA al referenciarlo, no devuelve\n * vacio. */\nfunction de(nombre) {\n  try { var x = $(nombre).first().json; return (x && typeof x === 'object') ? x : null; }\n  catch (e) { return null; }\n}\n\nvar d = de('Code - Que devolvio Odoo');\nif (!d || d.creo !== false) d = null;\nif (!d) { var a = de('Code - Armar orden'); if (a && a.armo === false) d = a; }\nif (!d) d = de('Code - Decidir') || {};\n\nreturn [{ json: {\n  ok: d.ok === true,\n  orden_creada: false,\n  ya_existia: d.ya_existia === true,\n  odoo_so_id: d.odoo_so_id || null,\n  odoo_so_name: d.odoo_so_name || null,\n  error: d.error || null,\n  faltan: d.faltan || null,\n  suma_lineas: (d.suma_lineas === undefined ? null : d.suma_lineas),\n  total_machote: (d.total_machote === undefined ? null : d.total_machote),\n  version_leida: (d.version_leida === undefined ? null : d.version_leida),\n  version_servidor: (d.version_servidor === undefined ? null : d.version_servidor),\n  plazo_pedido: (d.plazo_pedido === undefined ? null : d.plazo_pedido),\n  terminos_disponibles: d.terminos_disponibles || null,\n  mensaje: d.mensaje || 'No se creo la orden.'\n} }];\n" } }
});

const responder = node({
  type: 'n8n-nodes-base.respondToWebhook', version: 1.5,
  config: { name: 'Respond', parameters: { options: {} } }
});

export default workflow('comercial-orden-crear-v2', 'comercial/orden-crear-v2')
  .add(webhook)
  .to(setSecreto)
  .to(verificar)
  .to(ifToken
    .onTrue(reservar
      .to(getListas)
      .to(getImpuestos)
      .to(getTerminos)
      .to(getIncoterms)
      .to(decidir)
      .to(ifCrear
        .onTrue(productos
          .to(crearProducto)
          .to(armar)
          .to(ifArmo
            .onTrue(crearOrden
              .to(queDevolvio)
              .to(ifSeCreo
                .onTrue(releer.to(guardar).to(resultado).to(responder))
                .onFalse(soltar.to(noSeCreo).to(responder))))
            .onFalse(soltar)))
        .onFalse(soltar)))
    .onFalse(responder));
